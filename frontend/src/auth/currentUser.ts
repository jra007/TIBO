// TODO: replace with the authenticated user's id once auth is wired up.
// Same placeholder id used everywhere in the app so private ownership stays consistent
// across pages until real sessions exist.
export const CURRENT_USER_ID = 'admin-local';
